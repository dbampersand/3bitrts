local maxHP = 100

function setup()
    SetSprite("assets/encounters/tutorial/boss/doll.png");

    SetDamage(10);
    SetMaxHP(maxHP,true)
    SetSpeed(10)
end

function update(dt)
end

function kill()

end